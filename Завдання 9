#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>

int main()
{
 int dim=14, i;
 int A[dim];
 printf("Введіть масив A:\n");
 for(i=0;i<dim;i++)
 
 {
     printf(" A[%i] = ",i);
     scanf("%i",&A[i]);
 }
 }
